scripts:
  dev:
    cmd: deployctl run ./server.ts
    desc: Start dev server
    watch: true
  fmt:
    cmd: deno fmt --ignore=README.md,docs/
    desc: format code
  lint:
    cmd: deno lint
    desc: lint code